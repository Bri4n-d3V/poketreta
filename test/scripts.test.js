/**
 * @jest-environment jsdom
 */

const {
    fetchPkmn,
    enemyPkmn,
    friendPkmn1,
    friendPkmn2,
    friendPkmn3,
} = require("../src/script");

test('teste', () => {
    expect('oi').toBe('oi');
})